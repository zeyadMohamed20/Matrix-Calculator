#pragma once
#include<vector>
using namespace std;

/*
This function is used to turn matrix so that it takes only one parameter which is the wanted matrix to turn
then the function returns the matrix after transpose 
*/
vector<vector<double>>turn(vector<vector<double>>&mat);

/*
This function is used yo print matrix after transpose so that it takes no arguments
and returns void
*/
void print_matrix_transpose();
